'use client';

import type { Experience } from '@/lib/experienceTypes';

export default function SkillsMobile({
	className = '',
	skills,
}: {
	className?: string;
	skills: Experience[];
}) {
	return <></>;
}
